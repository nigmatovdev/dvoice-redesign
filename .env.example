# Copy to .env.local and fill values. Server-only — never prefix with NEXT_PUBLIC_.
# Signup-request proxy (src/app/api/signup-requests/route.ts)
SIGNUP_UPSTREAM_URL=https://[YOUR_DOMAIN]/api/signup-requests
SIGNUP_API_KEY=
